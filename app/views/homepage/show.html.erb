<h1>Products</h1>

<table>
  <% @products.each do |product| %>
  <tr>
    <td>
      <p><%= product.title %></p>
      <p><%= product.description %></p>
      <p><%= product.stock_quantity %> in stock</p>
    </td>
  </tr>
  <% end %>
</table>
